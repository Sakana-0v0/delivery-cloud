spring:
  application:
    name: del-file
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/del_file_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD:sakana013}
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 123456

minio:
  endpoint: ${MINIO_ENDPOINT:http://127.0.0.1:9000}
  access-key: ${MINIO_ACCESS_KEY:admin}
  secret-key: ${MINIO_SECRET_KEY:sakana013}
  bucket: ${MINIO_BUCKET:product-picture}

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml