<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakana.file.dao.mapper.FileInfoMapper">

    <select id="findByMd5" resultType="com.sakana.file.dao.entity.FileInfo">
        SELECT id, md5, original_name, file_name, file_url, file_size,
               content_type, file_type, create_time, is_deleted
        FROM t_file_info
        WHERE md5 = #{md5} AND is_deleted = 0
    </select>

</mapper>
